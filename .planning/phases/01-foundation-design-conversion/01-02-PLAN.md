---
phase: 01-foundation-design-conversion
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - components/nav.tsx
  - components/footer.tsx
  - app/layout.tsx
autonomous: true
requirements:
  - DSGN-02
  - DSGN-03

must_haves:
  truths:
    - "Every page shows the same Nav with logo, nav links (Home, Services, Schedule, Checkout), and a Book Now CTA button"
    - "Every page shows the same Footer with service links, company links, support links, and copyright"
    - "Nav has a glassmorphism backdrop effect matching design-3"
    - "Mobile menu toggle works on small screens"
  artifacts:
    - path: "components/nav.tsx"
      provides: "Shared navigation component with logo, links, Book Now CTA, mobile hamburger"
      min_lines: 40
    - path: "components/footer.tsx"
      provides: "Shared footer with 4 column layout (brand, services, company, support)"
      min_lines: 30
    - path: "app/layout.tsx"
      provides: "Root layout rendering Nav and Footer around page content"
      contains: "Nav"
  key_links:
    - from: "app/layout.tsx"
      to: "components/nav.tsx"
      via: "import and render in layout body"
      pattern: "import.*Nav.*from"
    - from: "app/layout.tsx"
      to: "components/footer.tsx"
      via: "import and render in layout body"
      pattern: "import.*Footer.*from"
---

<objective>
Create the shared Nav and Footer components extracted from design-3 HTML and wire them into the root layout so every page inherits them.

Purpose: Nav and Footer are shared chrome that every page needs. Building them first prevents duplication and ensures visual consistency across all page conversions.
Output: components/nav.tsx, components/footer.tsx, updated app/layout.tsx.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@docs/design-3-homepage-step-1/code.html
@docs/design-3-homepage-step-1/screen.png
@docs/main-logo/screen.png
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared Nav component</name>
  <files>components/nav.tsx</files>
  <read_first>
    - docs/design-3-homepage-step-1/code.html (extract nav HTML structure, classes, links)
    - docs/design-3-homepage-step-1/screen.png (visual reference for nav appearance)
    - docs/main-logo/screen.png (logo image reference)
    - app/globals.css (glass-card-nav utility class available from Plan 01)
    - components/ui/button.tsx (Button component pattern to reuse for CTA)
  </read_first>
  <action>
Create `components/nav.tsx` as a Server Component (no "use client" on the main component).

Structure from design-3 homepage HTML:
- Fixed/sticky top nav bar with `glass-card-nav` class for glassmorphism effect
- Left: Logo image (use `next/image` with the logo from `public/` -- copy `docs/main-logo/screen.png` to `public/logo.png` first) + "Monica's Miracle Mop" text
- Center: Navigation links -- Home (`/`), Services (`/services`), Schedule (`/schedule`), Checkout (`/checkout`)
- Right: "Book Now" CTA button using the `Button` component with `bg-[--primary-container]` styling (the #6a359c purple from design-3's CTA buttons), rounded-full, white text
- Links use `next/link` for client-side navigation
- Nav links use `text-foreground` default, `text-primary` or `font-semibold` for active state
- Use semantic Tailwind classes only (bg-primary, text-foreground, etc.)

Mobile: Create a separate client component `components/mobile-nav-toggle.tsx` with `"use client"` that:
- Shows a hamburger icon (Lucide `Menu` icon) on screens < `md` breakpoint
- Toggles a mobile menu dropdown with the same nav links stacked vertically
- Uses `useState` for open/close state
- Close icon uses Lucide `X`

The main Nav component imports and renders `MobileNavToggle` for small screens, hiding the desktop nav links with `hidden md:flex`.

Use `cn()` for all conditional classes. Use `data-slot="nav"` on the root element.
  </action>
  <verify>
    <automated>test -f components/nav.tsx && test -f components/mobile-nav-toggle.tsx && grep "glass-card-nav" components/nav.tsx && grep "use client" components/mobile-nav-toggle.tsx && grep "Book Now" components/nav.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/nav.tsx exists and does NOT contain "use client"
    - components/mobile-nav-toggle.tsx exists and contains "use client"
    - components/nav.tsx contains `glass-card-nav` class reference
    - components/nav.tsx contains `data-slot="nav"`
    - components/nav.tsx contains links to `/`, `/services`, `/schedule`, `/checkout`
    - components/nav.tsx contains "Book Now" CTA text
    - components/nav.tsx imports from `next/link`
    - components/nav.tsx imports from `next/image`
    - components/mobile-nav-toggle.tsx imports `Menu` and `X` from `lucide-react`
    - public/logo.png exists (copied from docs/main-logo/screen.png)
  </acceptance_criteria>
  <done>Nav component renders with glassmorphism effect, logo, 4 nav links, Book Now CTA, and mobile hamburger toggle</done>
</task>

<task type="auto">
  <name>Task 2: Create shared Footer component and wire Nav/Footer into layout</name>
  <files>components/footer.tsx, app/layout.tsx</files>
  <read_first>
    - docs/design-3-homepage-step-1/code.html (extract footer HTML structure -- look for the footer section with 4 column layout)
    - docs/design-3-homepage-step-1/screen.png (visual reference for footer)
    - app/layout.tsx (current layout to add Nav and Footer imports)
    - components/nav.tsx (already created in Task 1, to import)
  </read_first>
  <action>
1. Create `components/footer.tsx` as a Server Component (no "use client").

Structure from design-3 homepage HTML footer:
- Dark background section using `bg-foreground` (the dark colour #191c1f) with `text-background` (white text)
- 4-column grid layout on desktop (`grid md:grid-cols-4 gap-8`), stacking on mobile
- Column 1 (Brand): Logo + "Monica's Miracle Mop" + brief tagline
- Column 2 (Services): Links to Standard Clean, Deep Clean, Move-In/Out Clean (link to `/services` for now)
- Column 3 (Company): Links to About, Terms & Conditions (`/terms`), Privacy Policy
- Column 4 (Support): Phone number, email address, service area text
- Bottom bar: Copyright "2024 Monica's Miracle Mop. All rights reserved." with a `Separator` component above it
- Use `next/link` for all internal links
- Use `data-slot="footer"` on root element
- All copy in UK English

2. Update `app/layout.tsx`:
- Add imports: `import { Nav } from "@/components/nav"` and `import { Footer } from "@/components/footer"`
- Render Nav above `{children}` and Footer below in the body:
```tsx
<body className="min-h-full flex flex-col">
  <Nav />
  <main className="flex-1">{children}</main>
  <Footer />
</body>
```
- The `<main>` wrapper with `flex-1` ensures footer stays at bottom
  </action>
  <verify>
    <automated>test -f components/footer.tsx && grep "data-slot" components/footer.tsx && grep "Nav" app/layout.tsx && grep "Footer" app/layout.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/footer.tsx exists and does NOT contain "use client"
    - components/footer.tsx contains `data-slot="footer"`
    - components/footer.tsx contains "Monica's Miracle Mop"
    - components/footer.tsx contains `/terms` link
    - components/footer.tsx contains "All rights reserved"
    - app/layout.tsx contains `import { Nav }` or `import Nav`
    - app/layout.tsx contains `import { Footer }` or `import Footer`
    - app/layout.tsx contains `<Nav` and `<Footer`
    - app/layout.tsx contains `<main`
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Footer component renders with 4-column layout, Nav and Footer are wired into root layout, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Navigating to `http://localhost:3000` shows Nav at top and Footer at bottom
- Nav shows logo, 4 links, and Book Now CTA
- Footer shows 4-column layout with correct content
- Mobile viewport shows hamburger menu that toggles nav links
</verification>

<success_criteria>
- Shared Nav and Footer are rendered on every page via root layout
- Nav has glassmorphism effect, correct links, and mobile responsive toggle
- Footer has 4-column layout with UK English copy
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-02-SUMMARY.md`
</output>
