---
phase: 01-foundation-design-conversion
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - components/nav.tsx
  - components/footer.tsx
  - app/schedule/page.tsx
autonomous: true
gap_closure: true
requirements:
  - DSGN-02
  - DSGN-03
  - DSGN-06
  - DSGN-10

must_haves:
  truths:
    - "Nav shows exactly three links: Services, Reviews, About — no Home or Checkout link"
    - "Footer uses a light background matching design-3 (bg-slate-50 equivalent), not a dark bg-foreground"
    - "Schedule page container uses max-w-7xl px-4 md:px-8, consistent with all other pages"
  artifacts:
    - path: "components/nav.tsx"
      provides: "Nav with design-3 links"
      contains: "Reviews"
    - path: "components/footer.tsx"
      provides: "Footer with light background"
      contains: "bg-muted"
    - path: "app/schedule/page.tsx"
      provides: "Schedule with consistent container"
      contains: "max-w-7xl"
  key_links:
    - from: "components/nav.tsx"
      to: "design-3 nav"
      via: "navLinks array"
      pattern: "Services.*Reviews.*About"
    - from: "components/footer.tsx"
      to: "design-3 footer"
      via: "className on footer element"
      pattern: "bg-muted"
---

<objective>
Fix shared component visual gaps (Nav links, Footer background) and container width inconsistency (Schedule page) identified in verification.

Purpose: Close 3 verification gaps that affect every page (Nav, Footer are in root layout) and one page-specific container mismatch.
Output: Corrected Nav, Footer, and Schedule page matching design-3.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-conversion/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Nav links and Footer background to match design-3</name>
  <files>components/nav.tsx, components/footer.tsx</files>
  <read_first>
    - docs/design-3-homepage-step-1/code.html (lines 88-99 for nav, lines 375-410 for footer)
    - components/nav.tsx
    - components/footer.tsx
  </read_first>
  <action>
**Nav (components/nav.tsx):**
1. Change the `navLinks` array FROM:
```ts
const navLinks: { href: Route; label: string }[] = [
  { href: "/", label: "Home" },
  { href: "/services", label: "Services" },
  { href: "/schedule" as Route, label: "Schedule" },
  { href: "/checkout" as Route, label: "Checkout" },
]
```
TO:
```ts
const navLinks: { href: Route; label: string }[] = [
  { href: "/services", label: "Services" },
  { href: "#reviews" as Route, label: "Reviews" },
  { href: "#about" as Route, label: "About" },
]
```
This matches the design-3 HTML nav which shows exactly: Services, Reviews, About. Home and Checkout are NOT in the design-3 nav — the logo serves as the home link, and checkout is reached through the booking flow.

**Footer (components/footer.tsx):**
1. Change the footer element's className FROM:
   `className="w-full bg-foreground text-background"`
   TO:
   `className="w-full bg-muted border-t border-border"`
   The design-3 footer uses `bg-slate-50 border-t border-slate-100` — our semantic equivalents are `bg-muted` (maps to surface-container-low #f2f3f8) and `border-border`.

2. Update ALL text colour classes inside the footer to work on a light background:
   - `text-background` on the brand name → `text-foreground`
   - `text-background/70` on paragraph and links → `text-muted-foreground`
   - `text-background/50` on heading labels → `text-muted-foreground/70`
   - `text-background/50` on copyright → `text-muted-foreground`
   - `bg-background/10` on separator → `bg-border`
   - Remove `brightness-0 invert` from the logo Image (no longer needed on light bg)
   - Link hover states: `hover:text-background` → `hover:text-primary` (matches design-3's `hover:text-purple-600`)
  </action>
  <verify>
    <automated>cd "F:/Projects/Git/monicas-miracle-mop" && grep -c "Home\|Checkout" components/nav.tsx | grep "^0$" && grep "Reviews" components/nav.tsx && grep "About" components/nav.tsx && grep "bg-muted" components/footer.tsx && grep -c "bg-foreground" components/footer.tsx | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/nav.tsx navLinks array contains exactly Services, Reviews, About (no Home, no Checkout)
    - components/footer.tsx uses bg-muted (not bg-foreground) on the footer element
    - All footer text classes reference foreground/muted-foreground (not background)
    - Logo image in footer has no brightness-0 invert classes
  </acceptance_criteria>
  <done>Nav shows Services/Reviews/About links matching design-3. Footer has light bg-muted background with appropriate text colours for a light theme.</done>
</task>

<task type="auto">
  <name>Task 2: Standardise Schedule page container to match other pages</name>
  <files>app/schedule/page.tsx</files>
  <read_first>
    - app/schedule/page.tsx
    - docs/design-3-services-step-3/code.html (line 104 for container reference)
  </read_first>
  <action>
In `app/schedule/page.tsx`, change the container div className FROM:
```
className="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8"
```
TO:
```
className="mx-auto max-w-7xl px-4 md:px-8"
```
This matches the container pattern used on all other pages (homepage, services, checkout) and in the design-3 HTML (`max-w-7xl mx-auto` with `px-8`). The breakpoints also align: other pages use `md:px-8`, not `sm:px-6 lg:px-8`.
  </action>
  <verify>
    <automated>cd "F:/Projects/Git/monicas-miracle-mop" && grep "max-w-7xl" app/schedule/page.tsx && grep -c "max-w-6xl" app/schedule/page.tsx | grep "^0$" && grep "md:px-8" app/schedule/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app/schedule/page.tsx contains max-w-7xl (not max-w-6xl)
    - app/schedule/page.tsx contains px-4 md:px-8 (not sm:px-6 lg:px-8)
  </acceptance_criteria>
  <done>Schedule page container matches all other pages: max-w-7xl px-4 md:px-8</done>
</task>

</tasks>

<verification>
- grep for "max-w-6xl" across all page files returns 0 results
- grep for "bg-foreground" in footer.tsx returns 0 results
- grep for "Home" in nav.tsx navLinks returns 0 results
- `npm run build` completes without errors
</verification>

<success_criteria>
- Nav displays Services, Reviews, About (matching design-3 exactly)
- Footer has a light background (bg-muted) with proper text contrast
- All four pages use identical container width (max-w-7xl) and padding (px-4 md:px-8)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-09-SUMMARY.md`
</output>
