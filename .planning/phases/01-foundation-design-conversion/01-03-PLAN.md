---
phase: 01-foundation-design-conversion
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - DSGN-04

must_haves:
  truths:
    - "Homepage shows a hero section with gradient text heading, subheading, and Book Now CTA"
    - "Homepage shows 3 pricing cards (Standard, Deep, Move-In/Out) with GBP prices"
    - "Homepage shows a Monica Difference section with stat bubbles"
    - "Homepage shows a testimonials section"
    - "Homepage shows a final CTA section"
  artifacts:
    - path: "app/page.tsx"
      provides: "Complete homepage matching design-3 layout with hero, pricing cards, Monica Difference, testimonials, CTA"
      min_lines: 100
  key_links:
    - from: "app/page.tsx"
      to: "components/ui/card.tsx"
      via: "Card components for pricing cards"
      pattern: "import.*Card.*from"
    - from: "app/page.tsx"
      to: "components/ui/button.tsx"
      via: "Button for CTAs"
      pattern: "import.*Button.*from"
---

<objective>
Convert the design-3 homepage HTML into a Next.js Server Component page with all sections: hero with gradient text, pricing cards, Monica Difference stats, testimonials, and CTA.

Purpose: The homepage is the primary landing page and sets the visual standard for all other pages.
Output: Fully converted app/page.tsx matching the design-3 homepage screenshot.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@docs/design-3-homepage-step-1/code.html
@docs/design-3-homepage-step-1/screen.png
@docs/kasey-generated-flyers/flyer.png
@app/globals.css
@components/ui/card.tsx
@components/ui/button.tsx
@components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert homepage hero and pricing cards sections</name>
  <files>app/page.tsx</files>
  <read_first>
    - docs/design-3-homepage-step-1/code.html (full homepage HTML -- extract hero section and pricing cards section)
    - docs/design-3-homepage-step-1/screen.png (visual reference for exact layout)
    - docs/kasey-generated-flyers/flyer.png (real GBP pricing: Standard £20/hr, Deep Clean £100-£200)
    - app/globals.css (available theme tokens and glass-card utilities)
    - components/ui/card.tsx (Card component API to use for pricing cards)
    - components/ui/button.tsx (Button component for CTAs)
    - components/ui/badge.tsx (Badge component for "Most Popular" tag on Deep Clean card)
  </read_first>
  <action>
Replace the entire `app/page.tsx` with a Server Component (no "use client") containing the homepage.

**Hero Section:**
- Full-width section with a gradient or purple-toned background
- Large heading with gradient text effect (use CSS `bg-gradient-to-r from-primary to-secondary bg-clip-text text-transparent` or extract the exact gradient from design-3 HTML)
- Subheading text describing Monica's services
- "Book Now" CTA button using Button component with `bg-[--primary-container] text-white rounded-full px-8 py-3 text-lg font-semibold` styling
- Link the CTA to `/services`
- Hero stat bubble using `glass-card` class showing a key metric (e.g. "500+ Happy Homes" or similar from design-3)

**Pricing Cards Section:**
- Section heading: "Our Services" or matching design-3 text
- 3 cards in a responsive grid (`grid md:grid-cols-3 gap-6`)
- Each card uses the shadcn `Card` component (`CardHeader`, `CardTitle`, `CardContent`, `CardFooter`)
- Card 1 - Standard Clean: "From £20/hr", description of what's included, "Book Now" button linking to `/services`
- Card 2 - Deep Clean: "From £100", `Badge` with "Most Popular", featured styling with `bg-[--primary-container]` background and white text, "Book Now" button
- Card 3 - Move-In/Out Clean: "From £150", description, "Book Now" button
- All prices in GBP (£) per decision D-13
- Use Lucide icons for card decorations (e.g. `Sparkles`, `Home`, `Truck` from lucide-react)
- Use `data-icon` attribute on icon wrappers per shadcn icon conventions

**Use `next/image`** for any stock photos with `fill` prop in positioned containers. Download 2-3 royalty-free cleaning/home images from Unsplash URLs and save to `public/images/` directory. Use descriptive filenames like `hero-clean-home.jpg`.

All copy must use UK English spelling (e.g. "organised", "specialised").
  </action>
  <verify>
    <automated>grep -c "Card" app/page.tsx && grep "£" app/page.tsx | head -5 && grep "Book Now" app/page.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx does NOT contain "use client"
    - app/page.tsx contains `import` of Card components from `@/components/ui/card`
    - app/page.tsx contains `import` of Button from `@/components/ui/button`
    - app/page.tsx contains "Standard Clean" text
    - app/page.tsx contains "Deep Clean" text
    - app/page.tsx contains "Move-In/Out Clean" or "Move In/Out Clean" text
    - app/page.tsx contains at least 3 occurrences of "£" (GBP pricing)
    - app/page.tsx contains "£20" (standard clean price)
    - app/page.tsx contains `next/image` import
    - app/page.tsx contains at least one `lucide-react` import
  </acceptance_criteria>
  <done>Hero section with gradient heading and CTA, and 3 pricing cards with GBP prices are rendered on the homepage</done>
</task>

<task type="auto">
  <name>Task 2: Add Monica Difference, testimonials, and CTA sections</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current state from Task 1 -- append sections below pricing cards)
    - docs/design-3-homepage-step-1/code.html (extract Monica Difference section, testimonials section, and bottom CTA)
    - docs/design-3-homepage-step-1/screen.png (visual reference for these sections)
  </read_first>
  <action>
Add the remaining homepage sections below the pricing cards in `app/page.tsx`:

**"The Monica Difference" Section:**
- Section with a light background (`bg-muted` or `bg-primary-soft`)
- Heading: "The Monica Difference" (or exact text from design-3 HTML)
- 3-4 stat bubbles using `glass-card` class, each showing a metric:
  - Extract exact stats from design-3 HTML (e.g. "100% Satisfaction", "DBS Checked", "Fully Insured", "No Deposit Required")
- Each bubble has a Lucide icon above the stat text (e.g. `ShieldCheck`, `Award`, `HandCoins`)
- Grid layout: `grid grid-cols-2 md:grid-cols-4 gap-4`

**Testimonials Section:**
- Section heading: "What Our Customers Say" or matching design-3 text
- 2-3 testimonial cards using `Card` component
- Each testimonial has: quote text, customer name, star rating (use Lucide `Star` icon filled)
- Use placeholder testimonial content that matches the tone from design-3
- Cards in a responsive grid: `grid md:grid-cols-3 gap-6`

**Bottom CTA Section:**
- Full-width section with `bg-primary` background and `text-primary-foreground`
- Heading encouraging booking
- "Get Started" or "Book Your Clean" CTA button (white/outlined style on purple background)
- Link to `/services`

All text in UK English.
  </action>
  <verify>
    <automated>grep "Monica Difference\|Testimonial\|What Our" app/page.tsx && grep "ShieldCheck\|Award\|Star" app/page.tsx | head -3 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx contains "Monica Difference" or "The Monica Difference" text
    - app/page.tsx contains testimonial section with at least 2 customer quotes
    - app/page.tsx contains a bottom CTA section with `bg-primary`
    - app/page.tsx contains `glass-card` class usage for stat bubbles
    - app/page.tsx imports at least 3 Lucide icons (e.g. ShieldCheck, Star, Award or similar)
    - `npm run build` completes without errors
    - app/page.tsx total line count > 100
  </acceptance_criteria>
  <done>Complete homepage with all 5 sections (hero, pricing cards, Monica Difference, testimonials, CTA) matching design-3 layout</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Homepage at `http://localhost:3000` shows all 5 sections in correct order
- Pricing cards display GBP amounts
- Glass-card effect visible on stat bubbles
- All text uses UK English
</verification>

<success_criteria>
- Homepage visually matches design-3 screenshot layout
- All 5 sections present: hero, pricing, Monica Difference, testimonials, CTA
- GBP pricing displayed correctly
- Server Component (no "use client" on page)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-03-SUMMARY.md`
</output>
