---
phase: 01-foundation-design-conversion
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/services/page.tsx
  - components/booking/booking-summary.tsx
autonomous: true
requirements:
  - DSGN-05

must_haves:
  truths:
    - "Services page shows 3 service selection cards (Standard, Deep, Move-In/Out) with descriptions and GBP prices"
    - "Services page shows an add-on grid with optional extras"
    - "Services page shows a BookingSummary sidebar with static placeholder data"
    - "BookingSummary component is reusable and imported on both Services and Checkout pages"
  artifacts:
    - path: "app/services/page.tsx"
      provides: "Services page with service cards, add-on grid, and sidebar layout"
      min_lines: 80
    - path: "components/booking/booking-summary.tsx"
      provides: "Shared BookingSummary component with glass-card styling, static placeholder data"
      min_lines: 30
  key_links:
    - from: "app/services/page.tsx"
      to: "components/booking/booking-summary.tsx"
      via: "import and render in sidebar"
      pattern: "import.*BookingSummary.*from"
---

<objective>
Convert the design-3 services page HTML into a Next.js page with service selection cards, add-on grid, and a shared BookingSummary sidebar component.

Purpose: Services page is step 1 of the booking flow. The BookingSummary component created here is reused on Checkout (Plan 06).
Output: app/services/page.tsx, components/booking/booking-summary.tsx.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@docs/design-3-services-step-3/code.html
@docs/design-3-services-step-3/screen.png
@docs/kasey-generated-flyers/flyer.png
@app/globals.css
@components/ui/card.tsx
@components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BookingSummary shared component</name>
  <files>components/booking/booking-summary.tsx</files>
  <read_first>
    - docs/design-3-services-step-3/code.html (extract the booking summary sidebar HTML and structure)
    - docs/design-3-services-step-3/screen.png (visual reference for sidebar appearance)
    - docs/design-3-checkout-step-4/code.html (confirm sidebar appears on checkout too -- same structure)
    - docs/kasey-generated-flyers/flyer.png (real GBP pricing for placeholder data)
    - app/globals.css (glass-card-sidebar utility class)
    - components/ui/button.tsx (Button pattern reference)
  </read_first>
  <action>
Create `components/booking/booking-summary.tsx` as a Server Component (no "use client" -- Phase 1 uses static data only; booking state comes in Phase 2).

Follow the shadcn composition pattern from the existing button.tsx:
- Use `data-slot="booking-summary"` on root element
- Accept `className?: string` prop, merge with `cn()`
- Export as named export: `export function BookingSummary`

Structure from design-3 sidebar:
- Root: `<aside>` with `glass-card-sidebar rounded-3xl p-8 border border-white/40` classes
- Heading: "Booking Summary" in `text-xl font-extrabold text-primary`
- Static placeholder content showing:
  - Service: "Deep Clean" with price "£150.00"
  - Add-ons section: "Oven Cleaning - £45.00", "Ironing Service - £30.00"
  - Separator line
  - Frequency: "One-time"
  - Subtotal: "£225.00"
  - Total: "£225.00" in larger bold text
- "Continue" button at bottom using Button component, full width
- All prices in GBP (£)

Use the `Separator` component from `@/components/ui/separator` between sections.

Add a comment at top: `// Phase 1: Static placeholder data. Phase 2 will connect to BookingContext.`
  </action>
  <verify>
    <automated>test -f components/booking/booking-summary.tsx && grep "glass-card-sidebar" components/booking/booking-summary.tsx && grep "£" components/booking/booking-summary.tsx && grep "data-slot" components/booking/booking-summary.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/booking-summary.tsx exists
    - File does NOT contain "use client"
    - File contains `data-slot="booking-summary"`
    - File contains `glass-card-sidebar` class
    - File contains `export function BookingSummary`
    - File contains `className?: string` in props
    - File contains `cn(` usage
    - File contains at least 3 occurrences of "£"
    - File contains "Booking Summary" text
    - File contains "Phase 1" or "Phase 2" comment about future state
  </acceptance_criteria>
  <done>BookingSummary component renders with glass-card sidebar styling and static GBP placeholder data, ready for reuse on Services and Checkout pages</done>
</task>

<task type="auto">
  <name>Task 2: Convert services page with cards, add-ons, and sidebar</name>
  <files>app/services/page.tsx</files>
  <read_first>
    - docs/design-3-services-step-3/code.html (full services page HTML -- extract service cards, add-on grid, layout)
    - docs/design-3-services-step-3/screen.png (visual reference)
    - docs/kasey-generated-flyers/flyer.png (real GBP pricing and add-on list)
    - components/booking/booking-summary.tsx (just created, import path and props)
    - components/ui/card.tsx (Card component API)
    - app/globals.css (theme tokens)
  </read_first>
  <action>
Create `app/services/page.tsx` as a Server Component.

Add Next.js metadata:
```tsx
export const metadata = {
  title: "Services | Monica's Miracle Mop",
  description: "Choose from our professional cleaning services: Standard Clean, Deep Clean, and Move-In/Out Clean.",
};
```

**Two-column layout:**
- Left column (wider, ~2/3): Service cards + Add-ons grid
- Right column (sidebar, ~1/3): BookingSummary component (sticky positioning)
- Use `grid lg:grid-cols-[1fr_380px] gap-8` or similar responsive layout
- On mobile, sidebar stacks below content

**Service Selection Cards (left column, top):**
- Section heading: "Choose Your Service" or matching design-3 text
- 3 service cards in a vertical stack or `grid gap-4`:
  1. Standard Clean: icon (Lucide `Sparkles`), title, description ("Regular maintenance cleaning..."), price "From £20/hr", "Select" button
  2. Deep Clean: icon (Lucide `SprayCan` or `Droplets`), title, description, price "From £100", "Select" button, `Badge` with "Most Popular"
  3. Move-In/Out Clean: icon (Lucide `Truck`), title, description, price "From £150", "Select" button
- Each card uses shadcn `Card` with `CardHeader`, `CardContent`, `CardFooter`
- In Phase 1, "Select" buttons are non-functional links (booking state comes in Phase 2)

**Add-Ons Grid (left column, below services):**
- Section heading: "Add-On Services" or matching design-3
- Grid of add-on option cards: `grid grid-cols-2 md:grid-cols-3 gap-3`
- Add-ons from the flyer:
  - Oven Cleaning (£45)
  - Fridge Cleaning (£30)
  - Ironing Service (£30)
  - Inside Windows (£25)
  - Laundry Service (£25)
  - Carpet Cleaning (from £40)
- Each add-on is a smaller card with icon, name, and price
- In Phase 1, add-ons are display-only (no toggle state)

**Sidebar (right column):**
- Import and render `<BookingSummary />` from `@/components/booking/booking-summary`
- Apply `sticky top-24` positioning so it follows scroll on desktop

All prices in GBP. All copy in UK English.
  </action>
  <verify>
    <automated>test -f app/services/page.tsx && grep "BookingSummary" app/services/page.tsx && grep "£20" app/services/page.tsx && grep "£45" app/services/page.tsx && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/services/page.tsx exists and does NOT contain "use client"
    - File imports BookingSummary from `@/components/booking/booking-summary`
    - File contains "Standard Clean" text
    - File contains "Deep Clean" text
    - File contains "Move-In/Out" or "Move In/Out" text
    - File contains "£20" (standard clean price)
    - File contains "£45" (oven cleaning add-on price)
    - File contains at least 6 add-on service names
    - File contains metadata with title including "Services"
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Services page renders with 3 service cards, 6+ add-on options, and BookingSummary sidebar, all with correct GBP pricing</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/services` page shows service cards with GBP pricing
- BookingSummary sidebar renders with glass-card effect
- Add-ons grid shows 6 options with prices
- Layout is responsive (sidebar stacks on mobile)
</verification>

<success_criteria>
- Services page matches design-3 layout with two-column grid
- BookingSummary is a reusable component (not page-specific)
- All prices in GBP from Kasey's flyer
- Both files are Server Components
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-04-SUMMARY.md`
</output>
