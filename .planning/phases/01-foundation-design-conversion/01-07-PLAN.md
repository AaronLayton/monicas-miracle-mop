---
phase: 01-foundation-design-conversion
plan: 07
type: execute
wave: 5
depends_on: ["01-03", "01-04", "01-05", "01-06"]
files_modified:
  - app/terms/page.tsx
  - app/page.tsx
  - app/services/page.tsx
  - app/schedule/page.tsx
  - app/checkout/page.tsx
  - components/nav.tsx
  - components/footer.tsx
autonomous: true
requirements:
  - UK-01
  - UK-02
  - UK-04
  - DSGN-08
  - DSGN-09

must_haves:
  truths:
    - "All pricing across every page shows GBP (£) amounts using Kasey's real prices"
    - "All copy across every page uses UK English spelling (colour not color, organised not organized)"
    - "Terms & Conditions page exists at /terms with Kasey's cancellation policy and payment terms"
    - "No Google-hosted image URLs remain in any page"
    - "No Material Symbols references remain -- all icons are Lucide equivalents"
  artifacts:
    - path: "app/terms/page.tsx"
      provides: "Terms & Conditions page with cancellation policy, payment terms, and service agreement"
      min_lines: 50
  key_links:
    - from: "components/footer.tsx"
      to: "app/terms/page.tsx"
      via: "Link to /terms in footer Company column"
      pattern: "/terms"
---

<objective>
Create the Terms & Conditions page, then do a sweep across all pages to verify UK localisation (GBP pricing, UK English spelling) and confirm all images use local sources and all icons are Lucide.

Purpose: UK localisation is a cross-cutting concern that must be verified after all pages are built. The T&C page is a standalone requirement (UK-04).
Output: app/terms/page.tsx, verified UK-localised content across all pages.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@docs/kasey-generated-flyers/terms.jpg
@docs/kasey-generated-flyers/flyer.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Terms and Conditions page</name>
  <files>app/terms/page.tsx</files>
  <read_first>
    - docs/kasey-generated-flyers/terms.jpg (Kasey's actual T&C text to transcribe)
    - docs/kasey-generated-flyers/flyer.png (pricing reference to cross-check)
    - app/globals.css (theme tokens)
    - components/footer.tsx (verify /terms link exists in Company column)
  </read_first>
  <action>
Create `app/terms/page.tsx` as a Server Component.

Add metadata:
```tsx
export const metadata = {
  title: "Terms & Conditions | Monica's Miracle Mop",
  description: "Terms and conditions, cancellation policy, and payment terms for Monica's Miracle Mop cleaning services.",
};
```

Page structure:
- Heading: "Terms & Conditions" in `text-3xl font-extrabold text-primary`
- Last updated date below heading
- Content sections using semantic HTML (`<article>`, `<section>`, `<h2>`, `<p>`, `<ul>`)

Transcribe the actual content from `docs/kasey-generated-flyers/terms.jpg`. Key sections to include:
1. **Service Agreement** -- What's included in each service type
2. **Pricing & Payment** -- Kasey collects payment on day of clean (bank transfer or cash), no deposit required, pricing in GBP
3. **Cancellation Policy** -- Must include the specific cancellation window and fees from Kasey's terms
4. **Rescheduling** -- How to reschedule appointments
5. **Access & Keys** -- Policy on home access
6. **Liability & Insurance** -- Kasey is fully insured
7. **Contact** -- How to reach Monica's Miracle Mop

All content must:
- Use UK English spelling throughout
- Reference GBP prices where applicable
- Be factual and match Kasey's actual terms from the terms.jpg image
- Use the design-3 colour palette for headings (`text-primary`) and dividers (`border-border`)

Style with a clean reading layout: `max-w-3xl mx-auto px-4 py-12` with `space-y-8` between sections.
  </action>
  <verify>
    <automated>test -f app/terms/page.tsx && grep "Cancellation" app/terms/page.tsx && grep "£" app/terms/page.tsx && grep "Terms" app/terms/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - app/terms/page.tsx exists and does NOT contain "use client"
    - File contains "Terms & Conditions" or "Terms and Conditions" heading
    - File contains "Cancellation" section
    - File contains "Payment" section
    - File contains at least one "£" price reference
    - File contains metadata with title including "Terms"
    - File contains UK English text (no "color", "organize", "favor" spellings)
  </acceptance_criteria>
  <done>Terms & Conditions page renders with Kasey's actual cancellation policy, payment terms, and service agreement in UK English</done>
</task>

<task type="auto">
  <name>Task 2: UK localisation and image/icon sweep across all pages</name>
  <files>app/page.tsx, app/services/page.tsx, app/schedule/page.tsx, app/checkout/page.tsx, components/nav.tsx, components/footer.tsx</files>
  <read_first>
    - app/page.tsx (check for US spellings, USD prices, Google images, Material Symbols)
    - app/services/page.tsx (same checks)
    - app/schedule/page.tsx (same checks)
    - app/checkout/page.tsx (same checks)
    - components/nav.tsx (same checks)
    - components/footer.tsx (same checks)
    - docs/kasey-generated-flyers/flyer.png (verify all prices match flyer)
  </read_first>
  <action>
Do a systematic sweep of every page and shared component to verify and fix:

**1. GBP Pricing (UK-01):**
Verify these exact prices from Kasey's flyer appear correctly:
- Standard Clean: £20/hr
- Deep Clean: £100 - £200 (depending on property size)
- Move-In/Out Clean: £150+ (or from £150)
- Oven Cleaning: £45
- Fridge Cleaning: £30
- Ironing Service: £30
- Inside Windows: £25
- Laundry Service: £25
- Carpet Cleaning: from £40
Fix any price that doesn't match. Ensure no "$" or "USD" appears anywhere.

**2. UK English (UK-02):**
Search and replace these US English patterns across ALL files:
- "color" -> "colour" (in visible copy only, NOT in CSS/code identifiers)
- "organize" -> "organise"
- "specialized" -> "specialised"
- "customize" -> "customise"
- "favor" -> "favour"
- "center" (in copy) -> "centre"
- "canceled" -> "cancelled"
- "apartment" -> "flat" (where appropriate in UK context)
Note: Do NOT change CSS class names, variable names, or code identifiers -- only user-visible text content.

**3. Image Sources (DSGN-08):**
Search for any remaining `google` or `gstatic` or `googleapis` URLs in image src attributes. Replace with local images from `public/images/`. If images from Unsplash were downloaded in earlier plans, verify they exist and are referenced correctly via `next/image`.

**4. Icon Sources (DSGN-09):**
Search for any remaining `material-symbols` or `Material Icons` references. All should have been replaced with Lucide imports in earlier plans, but verify:
- No `<span class="material-symbols-*">` patterns
- No Google Fonts icon stylesheet links
- All icons use `import { IconName } from "lucide-react"` pattern

Report any issues found and fixed.
  </action>
  <verify>
    <automated>echo "=== USD check ===" && grep -r "\\\$[0-9]" app/ components/ --include="*.tsx" || echo "No USD found" && echo "=== Google images ===" && grep -ri "google\|gstatic\|googleapis" app/ components/ --include="*.tsx" || echo "No Google refs" && echo "=== Material Symbols ===" && grep -ri "material.symbols\|material.icons" app/ components/ --include="*.tsx" || echo "No Material Symbols" && echo "=== Build ===" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No file in app/ or components/ contains "$" followed by a number (no USD pricing)
    - No file contains "google" or "gstatic" or "googleapis" in image src attributes
    - No file contains "material-symbols" or "Material Icons" references
    - All visible copy uses UK English spellings (grep for "color" in quotes returns 0 matches in user-visible strings)
    - Pricing matches Kasey's flyer: £20, £100, £150, £45, £30, £25, £40 all present across relevant pages
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>All pages verified: GBP pricing matches flyer, UK English throughout, no Google-hosted images, no Material Symbols -- all icons are Lucide</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/terms` page renders with cancellation policy
- No US English spellings in user-visible text
- All prices in GBP matching Kasey's flyer
- No external Google dependencies for images or icons
</verification>

<success_criteria>
- Terms & Conditions page exists with real content from Kasey's terms
- UK localisation verified across all pages
- Image and icon migration complete
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-07-SUMMARY.md`
</output>
