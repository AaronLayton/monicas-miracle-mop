---
phase: 01-foundation-design-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "All pages use Plus Jakarta Sans font instead of Geist"
    - "Primary purple (#511983), secondary teal (#00687a), and all design-3 colours are available as semantic Tailwind classes"
    - "Glass-card effect (semi-transparent white + backdrop blur) is available as a utility class"
  artifacts:
    - path: "app/globals.css"
      provides: "Design-3 colour tokens mapped to shadcn semantic variables in OKLch format, glass-card utility, font variable mapping"
      contains: "--primary: oklch"
    - path: "app/layout.tsx"
      provides: "Plus Jakarta Sans font loaded via next/font/google, lang set to en-GB"
      contains: "Plus_Jakarta_Sans"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS variable --font-plus-jakarta-sans referenced in @theme inline --font-sans"
      pattern: "--font-plus-jakarta-sans"
---

<objective>
Establish the design system foundation: swap the font to Plus Jakarta Sans, remap all colour tokens from the default shadcn greyscale to the design-3 purple/teal palette, add the glass-card CSS utility, and install all shadcn UI primitives needed for Phase 1.

Purpose: Every subsequent plan depends on this design system being in place. Without correct tokens, all page conversions would use wrong colours.
Output: Updated globals.css with design-3 tokens, updated layout.tsx with correct font, shadcn components installed.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@app/globals.css
@app/layout.tsx
@components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remap colour tokens and add glass-card utility</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current default shadcn greyscale tokens to replace)
    - .planning/phases/01-foundation-design-conversion/01-RESEARCH.md (Pattern 1: token mapping table, Pattern 3: glass-card)
    - docs/design-3-homepage-step-1/code.html (source MD3 colour definitions)
  </read_first>
  <action>
Replace the entire `:root` block in `app/globals.css` with the design-3 colour palette mapped to shadcn semantic tokens in OKLch format. The exact values to use:

```
--background: oklch(0.989 0.004 264.5);    /* #f8f9fe surface */
--foreground: oklch(0.153 0.007 264.5);    /* #191c1f on-surface */
--primary: oklch(0.341 0.195 307.1);       /* #511983 purple */
--primary-foreground: oklch(1 0 0);        /* #ffffff */
--secondary: oklch(0.437 0.094 213.7);     /* #00687a teal */
--secondary-foreground: oklch(1 0 0);      /* #ffffff */
--accent: oklch(0.916 0.063 218.5);        /* #abedff light teal */
--accent-foreground: oklch(0.271 0.071 213.7); /* #006172 */
--muted: oklch(0.963 0.006 264.5);         /* #f2f3f8 surface-container-low */
--muted-foreground: oklch(0.384 0.027 307.1);  /* #4c4451 */
--card: oklch(1 0 0);                      /* #ffffff */
--card-foreground: oklch(0.153 0.007 264.5);   /* #191c1f */
--popover: oklch(1 0 0);
--popover-foreground: oklch(0.153 0.007 264.5);
--border: oklch(0.839 0.018 307.1);        /* #cec3d3 outline-variant */
--input: oklch(0.839 0.018 307.1);
--ring: oklch(0.341 0.195 307.1);          /* match primary */
--destructive: oklch(0.577 0.245 27.3);    /* #ba1a1a */
--radius: 0.75rem;                         /* design uses xl=0.75rem */
```

Add a custom token below the standard ones:
```
--primary-container: oklch(0.431 0.168 307.1); /* #6a359c for CTA buttons */
--primary-soft: oklch(0.918 0.062 307.1);      /* #f0dbff primary-fixed */
```

Remove the `.dark` block entirely (no dark mode for this project per out-of-scope decision).

Remove all `--sidebar-*`, `--chart-*` variables (not used in this project).

Update the `@theme inline` block:
- Change `--font-sans` to: `var(--font-plus-jakarta-sans), ui-sans-serif, system-ui, sans-serif;`
- Change `--font-heading` to: `var(--font-plus-jakarta-sans), ui-sans-serif, system-ui, sans-serif;`
- Remove `--font-mono` line (not used in design)
- Remove all `--color-sidebar-*` and `--color-chart-*` lines
- Add: `--color-primary-container: var(--primary-container);`
- Add: `--color-primary-soft: var(--primary-soft);`

Add glass-card utility after the `@layer base` block:
```css
@layer utilities {
  .glass-card {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
  }
  .glass-card-nav {
    background: rgba(255, 255, 255, 0.85);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
  }
  .glass-card-sidebar {
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }
}
```
  </action>
  <verify>
    <automated>npx tailwindcss --help > /dev/null 2>&1 && echo "tailwind ok" && grep -c "oklch" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `--primary: oklch(0.341 0.195 307.1)`
    - app/globals.css contains `--secondary: oklch(0.437 0.094 213.7)`
    - app/globals.css contains `--primary-container: oklch(0.431 0.168 307.1)`
    - app/globals.css contains `--primary-soft: oklch(0.918 0.062 307.1)`
    - app/globals.css contains `.glass-card {`
    - app/globals.css contains `.glass-card-nav {`
    - app/globals.css contains `.glass-card-sidebar {`
    - app/globals.css contains `--font-plus-jakarta-sans`
    - app/globals.css does NOT contain `--sidebar`
    - app/globals.css does NOT contain `.dark {`
    - app/globals.css does NOT contain `--chart-`
  </acceptance_criteria>
  <done>All design-3 colour tokens mapped to shadcn semantic variables, glass-card utilities defined, sidebar/chart/dark-mode tokens removed</done>
</task>

<task type="auto">
  <name>Task 2: Swap font to Plus Jakarta Sans and install shadcn components</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current Geist font imports to replace)
    - .planning/phases/01-foundation-design-conversion/01-RESEARCH.md (Pattern 2: font swap, Pattern 5: shadcn CLI)
    - components.json (shadcn config to confirm base-nova style)
  </read_first>
  <action>
1. In `app/layout.tsx`, replace the Geist font imports with Plus Jakarta Sans:

Remove:
```tsx
import { Geist, Geist_Mono } from "next/font/google";
const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });
const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });
```

Add:
```tsx
import { Plus_Jakarta_Sans } from "next/font/google";
const plusJakartaSans = Plus_Jakarta_Sans({
  subsets: ["latin"],
  variable: "--font-plus-jakarta-sans",
  display: "swap",
  weight: ["300", "400", "500", "600", "700", "800"],
});
```

2. Update the `<html>` tag:
- Change `lang="en"` to `lang="en-GB"` (UK localisation per D-14, Pitfall 6)
- Change className from `${geistSans.variable} ${geistMono.variable}` to `${plusJakartaSans.variable}`
- Keep `h-full antialiased`

3. Update metadata:
```tsx
export const metadata: Metadata = {
  title: "Monica's Miracle Mop | Professional Cleaning Services",
  description: "Professional house cleaning services. Standard cleans, deep cleans, and move-in/out cleans across the local area.",
};
```

4. Install shadcn UI components needed for Phase 1 via CLI (run each command):
```bash
npx shadcn@latest add card
npx shadcn@latest add input
npx shadcn@latest add label
npx shadcn@latest add badge
npx shadcn@latest add separator
```

Do NOT install: dialog, sheet, table, select, form, radio-group (not needed in Phase 1).
  </action>
  <verify>
    <automated>grep "Plus_Jakarta_Sans" app/layout.tsx && grep 'lang="en-GB"' app/layout.tsx && ls components/ui/card.tsx components/ui/input.tsx components/ui/label.tsx components/ui/badge.tsx components/ui/separator.tsx && npm run build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains `import { Plus_Jakarta_Sans } from "next/font/google"`
    - app/layout.tsx contains `variable: "--font-plus-jakarta-sans"`
    - app/layout.tsx contains `lang="en-GB"`
    - app/layout.tsx does NOT contain `Geist`
    - app/layout.tsx contains `Monica's Miracle Mop`
    - components/ui/card.tsx exists
    - components/ui/input.tsx exists
    - components/ui/label.tsx exists
    - components/ui/badge.tsx exists
    - components/ui/separator.tsx exists
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Font swapped to Plus Jakarta Sans, lang set to en-GB, metadata updated, all required shadcn components installed and ready for page conversion</done>
</task>

</tasks>

<verification>
- `npm run build` passes with no errors
- Browser renders text in Plus Jakarta Sans (visible in DevTools computed font)
- Tailwind classes `bg-primary`, `text-secondary`, `bg-primary-container` all resolve to design-3 colours
- `.glass-card` class produces visible backdrop blur effect
</verification>

<success_criteria>
- Design-3 colour palette is the active theme (purple/teal, not greyscale)
- Plus Jakarta Sans is the only font loaded
- Glass-card utility classes are available
- All required shadcn primitives (card, input, label, badge, separator) are installed
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-01-SUMMARY.md`
</output>
