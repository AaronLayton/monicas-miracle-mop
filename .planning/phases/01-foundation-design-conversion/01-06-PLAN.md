---
phase: 01-foundation-design-conversion
plan: 06
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - app/checkout/page.tsx
autonomous: true
requirements:
  - DSGN-07
  - UK-03

must_haves:
  truths:
    - "Checkout page shows a contact details form with name, email, and phone fields"
    - "Checkout page shows a service address form with UK address fields (Address Line 1, Address Line 2, Town/City, County, Postcode)"
    - "Checkout page shows a BookingSummary sidebar with static placeholder data"
    - "Checkout page has a Confirm Booking button (no payment section)"
    - "Address form uses UK format: no US State field, no ZIP field, has Postcode field"
  artifacts:
    - path: "app/checkout/page.tsx"
      provides: "Checkout page with contact form, UK address form, special notes, and BookingSummary sidebar"
      min_lines: 80
  key_links:
    - from: "app/checkout/page.tsx"
      to: "components/booking/booking-summary.tsx"
      via: "import and render in sidebar"
      pattern: "import.*BookingSummary"
    - from: "app/checkout/page.tsx"
      to: "components/ui/input.tsx"
      via: "Input components for form fields"
      pattern: "import.*Input"
---

<objective>
Convert the design-3 checkout page HTML into a Next.js page with contact details form, UK-formatted address form, and BookingSummary sidebar. Remove the payment section entirely per D-05.

Purpose: Checkout is the final step of the booking flow. UK address format (D-12) is implemented here.
Output: app/checkout/page.tsx with forms and sidebar.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-design-conversion/01-RESEARCH.md

@docs/design-3-checkout-step-4/code.html
@docs/design-3-checkout-step-4/screen.png
@app/globals.css
@components/booking/booking-summary.tsx
@components/ui/input.tsx
@components/ui/label.tsx
@components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create checkout page with contact form, UK address form, and sidebar</name>
  <files>app/checkout/page.tsx</files>
  <read_first>
    - docs/design-3-checkout-step-4/code.html (full checkout page HTML -- extract contact and address form structure, note payment section to remove)
    - docs/design-3-checkout-step-4/screen.png (visual reference)
    - components/booking/booking-summary.tsx (sidebar import path and interface)
    - components/ui/input.tsx (Input component API)
    - components/ui/label.tsx (Label component API)
    - components/ui/card.tsx (Card component for form sections)
    - app/globals.css (theme tokens)
  </read_first>
  <action>
Create `app/checkout/page.tsx` as a Server Component. The forms are display-only in Phase 1 (form submission comes in Phase 2).

Add metadata:
```tsx
export const metadata = {
  title: "Checkout | Monica's Miracle Mop",
  description: "Complete your booking with your contact details and service address.",
};
```

**Two-column layout** (consistent with Services and Schedule pages):
- Left column (~2/3): Contact form + Address form + Special notes + Confirm button
- Right column (~1/3): BookingSummary sidebar with `sticky top-24`
- `grid lg:grid-cols-[1fr_380px] gap-8`

**Section 1: Contact Details** (inside a `Card`):
- Heading: "Contact Details" with Lucide `User` icon
- Fields using shadcn `Input` + `Label` components:
  1. Full Name (type="text", placeholder="e.g. Jane Smith")
  2. Email Address (type="email", placeholder="e.g. jane@example.co.uk")
  3. Phone Number (type="tel", placeholder="e.g. 07700 900000")
- Layout: `grid grid-cols-1 md:grid-cols-2 gap-4` with Full Name spanning full width, Email and Phone side by side
- Use Lucide icons inline: `User`, `Mail`, `Phone`

**Section 2: Service Address** (inside a `Card`):
- Heading: "Service Address" with Lucide `MapPin` icon
- **UK address format per D-12** -- fields:
  1. Address Line 1 (required, placeholder="e.g. 42 Oak Avenue")
  2. Address Line 2 (optional, placeholder="e.g. Flat 3")
  3. Town/City (required, placeholder="e.g. Manchester")
  4. County (optional, placeholder="e.g. Greater Manchester")
  5. Postcode (required, placeholder="e.g. M1 2AB")
- Layout: Address Line 1 full width, Address Line 2 full width, Town/City and County side by side, Postcode half width
- Do NOT include: State field, ZIP code field, Country field (UK only)

**Section 3: Special Notes** (inside a `Card`):
- Heading: "Special Notes" with Lucide `MessageSquare` icon
- Single textarea field for additional instructions
- Placeholder: "e.g. Key is under the doormat, please avoid the conservatory"
- Use a `<textarea>` styled with Input classes (`cn("border-input bg-background ...")`)

**Confirm Booking Button:**
- Full-width `Button` at bottom of left column
- Text: "Confirm Booking"
- Style: `bg-[--primary-container] text-white rounded-full py-3 text-lg font-semibold w-full`
- In Phase 1: non-functional (no form action). Add comment: `// Phase 2: Connect to booking submission Server Action`

**No payment section** (removed per D-05). Do not include credit card fields, payment method selector, or any payment UI.

**Sidebar:**
- `<BookingSummary />` imported from `@/components/booking/booking-summary`

All placeholder text uses UK English and UK phone/postcode formats.
  </action>
  <verify>
    <automated>test -f app/checkout/page.tsx && grep "BookingSummary" app/checkout/page.tsx && grep "Postcode" app/checkout/page.tsx && grep "Confirm Booking" app/checkout/page.tsx && grep -v "payment\|credit card\|card number" app/checkout/page.tsx > /dev/null && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/checkout/page.tsx exists
    - File imports BookingSummary from `@/components/booking/booking-summary`
    - File imports Input from `@/components/ui/input`
    - File imports Label from `@/components/ui/label`
    - File contains "Contact Details" section
    - File contains "Service Address" section
    - File contains "Postcode" field label
    - File contains "Town/City" or "Town" field label
    - File does NOT contain "State" as a form field label
    - File does NOT contain "ZIP" text
    - File does NOT contain "payment" or "credit card" or "card number" (case-insensitive)
    - File contains "Confirm Booking" button text
    - File contains "Special Notes" section
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Checkout page renders with contact details form, UK-formatted address form, special notes textarea, Confirm Booking button, and BookingSummary sidebar. No payment section present.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `/checkout` page shows contact form, UK address form, and sidebar
- Address form has Postcode field (not ZIP), no State field
- No payment section visible
- BookingSummary sidebar renders with glass-card effect
</verification>

<success_criteria>
- Checkout page matches design-3 layout minus payment section
- UK address format implemented correctly (Postcode, Town/City, County, no State/ZIP)
- BookingSummary sidebar reused from shared component
- All placeholder text uses UK English and UK formats
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-06-SUMMARY.md`
</output>
