---
phase: 01-foundation-design-conversion
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/services/page.tsx
  - app/checkout/page.tsx
  - components/booking/booking-summary.tsx
autonomous: true
gap_closure: true
requirements:
  - DSGN-04
  - DSGN-05
  - DSGN-07
  - DSGN-10

must_haves:
  truths:
    - "Homepage featured pricing card (Deep Clean) uses bg-primary-container (lighter purple #6a359c), not bg-primary (dark #511983)"
    - "Services page service cards have NO Select/button elements — cards are clickable containers only"
    - "Services page non-highlighted card icon backgrounds use bg-accent (cyan), not bg-muted (grey)"
    - "Services page add-on grid uses grid-cols-2 sm:grid-cols-3 md:grid-cols-4 matching design-3"
    - "Checkout booking summary sidebar has decorative blur blobs and a date display row"
  artifacts:
    - path: "app/page.tsx"
      provides: "Homepage with correct featured card colour"
      contains: "bg-primary-container"
    - path: "app/services/page.tsx"
      provides: "Services page without Select buttons, correct icon bg"
      contains: "bg-accent"
    - path: "components/booking/booking-summary.tsx"
      provides: "Booking summary with decorative blobs and date display"
      contains: "blur-3xl"
  key_links:
    - from: "app/page.tsx"
      to: "ServiceCard component"
      via: "featured card bg class"
      pattern: "bg-primary-container"
    - from: "app/services/page.tsx"
      to: "design-3 service cards"
      via: "Card components without Button children"
      pattern: "bg-accent"
    - from: "components/booking/booking-summary.tsx"
      to: "design-3 checkout sidebar"
      via: "decorative blobs and date section"
      pattern: "blur-3xl"
---

<objective>
Fix page-specific visual gaps: Homepage featured card colour, Services page extra buttons and icon backgrounds, add-on grid columns, and Checkout sidebar missing decorative blobs and date display.

Purpose: Close all remaining visual fidelity gaps from verification so every page matches design-3 screenshots.
Output: Corrected Homepage, Services, and Checkout pages matching design-3.
</objective>

<execution_context>
@C:/Users/aaron/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/aaron/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-design-conversion/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Homepage featured pricing card and Services page cards</name>
  <files>app/page.tsx, app/services/page.tsx</files>
  <read_first>
    - docs/design-3-homepage-step-1/code.html (lines 153-215 for pricing cards section)
    - docs/design-3-services-step-3/code.html (lines 113-175 for service cards and add-ons grid)
    - app/page.tsx (lines 140-200 for ServiceCard component)
    - app/services/page.tsx
  </read_first>
  <action>
**Homepage (app/page.tsx) — ServiceCard component around line 152-157:**

In the ServiceCard function, change the featured card background FROM:
```
featured
  ? "bg-primary text-white scale-105 shadow-2xl shadow-primary/20 z-10"
  : "bg-card group hover:-translate-y-2"
```
TO:
```
featured
  ? "bg-primary-container text-white scale-105 shadow-2xl shadow-primary/20 z-10"
  : "bg-card group hover:-translate-y-2"
```

The design-3 HTML uses `bg-primary-container` (#6a359c, a lighter purple) on the featured Deep Clean card, NOT `bg-primary` (#511983, the dark purple). The `bg-primary-container` Tailwind class is already mapped in globals.css.

Also on the non-featured card icon container (around line 176), change FROM:
```
featured ? "bg-white/10 backdrop-blur" : "bg-accent"
```
Verify this is already `bg-accent` (matching design-3's `bg-secondary-fixed`). If it says `bg-muted`, change to `bg-accent`. The design shows the non-featured icon backgrounds as `bg-secondary-fixed` which maps to `bg-accent` in our theme.

**Services page (app/services/page.tsx):**

1. **Remove the Select buttons entirely.** Delete the entire `<CardFooter>` block (lines ~165-174) from inside the services.map() render. The design-3 service cards on the services page do NOT have Select buttons — the entire card is a clickable container. The design shows a "SELECTED" pill on the active card (border-2 border-primary), but no buttons.

2. **Fix non-highlighted card icon background.** In the icon container div (around line 137-139), change FROM:
   ```
   service.highlight ? "bg-primary/10" : "bg-muted"
   ```
   TO:
   ```
   service.highlight ? "bg-primary/10" : "bg-accent"
   ```
   The design-3 services page uses `bg-secondary-fixed` for non-selected card icon backgrounds, which maps to `bg-accent` in our tokens. Grey `bg-muted` does not match.

3. **Fix add-on grid columns.** Change the add-on grid FROM:
   ```
   className="grid grid-cols-2 md:grid-cols-3 gap-4"
   ```
   TO:
   ```
   className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4"
   ```
   This matches the design-3 HTML exactly: `grid-cols-2 sm:grid-cols-3 md:grid-cols-4`.
  </action>
  <verify>
    <automated>cd "F:/Projects/Git/monicas-miracle-mop" && grep "bg-primary-container" app/page.tsx && grep -c "CardFooter" app/services/page.tsx | grep "^0$" && grep "bg-accent" app/services/page.tsx && grep "md:grid-cols-4" app/services/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app/page.tsx ServiceCard featured branch uses bg-primary-container (not bg-primary)
    - app/services/page.tsx has zero CardFooter elements (Select buttons removed)
    - app/services/page.tsx non-highlighted icon bg uses bg-accent (not bg-muted)
    - app/services/page.tsx add-on grid uses grid-cols-2 sm:grid-cols-3 md:grid-cols-4
  </acceptance_criteria>
  <done>Homepage featured card is lighter purple (bg-primary-container). Services page has no Select buttons, correct icon colours, and 4-column add-on grid at md breakpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add decorative blobs and date display to BookingSummary for checkout</name>
  <files>components/booking/booking-summary.tsx</files>
  <read_first>
    - docs/design-3-checkout-step-4/code.html (lines 217-287 for the booking summary sidebar with blobs and date)
    - components/booking/booking-summary.tsx
  </read_first>
  <action>
The design-3 checkout sidebar has two features missing from our BookingSummary component:

**1. Decorative blur blobs (background accents):**

Add two absolutely-positioned decorative divs inside the aside element, BEFORE the heading. The aside already has relative positioning implied by the glass-card-sidebar. Add `relative overflow-hidden` to the aside className if not already present.

Add these two divs right after the opening aside tag:
```tsx
{/* Decorative background accents */}
<div className="absolute -top-12 -right-12 w-32 h-32 bg-accent/20 rounded-full blur-3xl" aria-hidden="true" />
<div className="absolute -bottom-12 -left-12 w-32 h-32 bg-primary-container/10 rounded-full blur-3xl" aria-hidden="true" />
```

This matches the design-3 HTML:
- `bg-secondary-container/20` → `bg-accent/20` (our semantic equivalent)
- `bg-primary-container/10` → `bg-primary-container/10` (same token)

Also add `relative z-10` to the content wrapper divs so they render above the blobs (the design does this with `relative z-10` on the space-y-6 div).

**2. Date & time display row:**

Add a date display section between the line items border-b div and the Totals section. This is static placeholder data for Phase 1 (like the rest of the summary). Insert this block:

```tsx
{/* Date & time (static placeholder - Phase 2 connects real data) */}
<div className="flex items-center gap-4 py-2">
  <div className="bg-muted rounded-xl p-3 flex flex-col items-center min-w-[64px]">
    <span className="text-[10px] font-bold uppercase text-primary">Jan</span>
    <span className="text-xl font-black text-foreground">15</span>
  </div>
  <div>
    <p className="font-bold text-foreground">Wednesday, 09:00 AM</p>
    <p className="text-sm text-muted-foreground flex items-center gap-1">
      <Clock className="size-3" aria-hidden="true" /> Est. 3-4 hours
    </p>
  </div>
</div>
<div className="h-px bg-border" aria-hidden="true" />
```

Add `Clock` to the lucide-react import at the top of the file:
```ts
import { ShoppingBasket, ArrowRight, ShieldCheck, ThumbsUp, Clock } from "lucide-react"
```

This matches the design-3 checkout sidebar which shows a date block with month abbreviation + day number, plus day-of-week and time, plus estimated duration.
  </action>
  <verify>
    <automated>cd "F:/Projects/Git/monicas-miracle-mop" && grep "blur-3xl" components/booking/booking-summary.tsx && grep "Clock" components/booking/booking-summary.tsx && grep "Est\." components/booking/booking-summary.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - components/booking/booking-summary.tsx contains two decorative div elements with blur-3xl class
    - components/booking/booking-summary.tsx contains a date display section with month, day number, time, and estimated duration
    - Clock icon is imported from lucide-react
    - The aside element has overflow-hidden to clip the blur blobs
  </acceptance_criteria>
  <done>Checkout sidebar matches design-3 with decorative blur blobs and a date/time display row.</done>
</task>

</tasks>

<verification>
- grep for "bg-primary" (exact, not bg-primary-container) in the ServiceCard featured branch returns 0
- grep for "CardFooter" in services/page.tsx returns 0
- grep for "bg-muted" as icon background in services/page.tsx returns 0 (should be bg-accent)
- grep for "blur-3xl" in booking-summary.tsx returns matches
- grep for "Clock" in booking-summary.tsx returns match
- `npm run build` completes without errors
</verification>

<success_criteria>
- Homepage Deep Clean card is lighter purple (bg-primary-container), visually matching design-3
- Services page cards have no Select buttons, matching design-3's clickable-card pattern
- Services page non-highlighted icon backgrounds are cyan (bg-accent), not grey
- Services add-on grid shows 4 columns at md breakpoint
- Checkout sidebar shows decorative blur blobs and a placeholder date/time row
- All changes verifiable against design-3 HTML source
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-design-conversion/01-10-SUMMARY.md`
</output>
